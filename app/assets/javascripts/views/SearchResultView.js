var app = app || {};

app.SearchResultView = Backbone.View.extend({
  tagName: 'li',
  render: function() {
    // var content = this.model.get('origin');
    // this.$el.text(content);
    // $('#searchResults').prepend( this.el );
  }
})
